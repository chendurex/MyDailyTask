1、JDK内存池与tomcat内存配合使用
2、在多个tomcat情况下，默认情况下，tomcat是否会各自申请内存，如果分配了内存后，情况又是怎样，各个tomcat是否会相互影响内存
3、tomcat怎样配置内存，它们之间应该怎么隔离，才能保证各个tomcat不会互相影响，它们的session又是怎样共享，其中一个宕机后，影响是怎样
4、JDK的配置是怎样的，才能使tomcat发挥最好的效果
5、window下跟linux的配置是否会有不同，linux下是否有其它影响配置的因素
6、tomcat的基本配置与基本的连接容器